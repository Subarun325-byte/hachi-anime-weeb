{
  "name": "hachi-anime-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fetch": "node scripts/fetch-mal.js",
    "dev": "npm run fetch && hugo server --buildDrafts --bind 0.0.0.0",
    "build": "npm run fetch && hugo --minify",
    "preview": "npm run build && npx serve public",
    "admin": "node admin/server.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "multer": "^2.1.1"
  }
}
